apiVersion: v1
kind: Service
metadata:
  labels:
    app: customermanagement
    environment: Development
  name: customermanagement
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: customermanagement
  sessionAffinity: None
  type: ClusterIP
